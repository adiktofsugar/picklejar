query GetPhotos($cursor: String) {
  photos(first: 100, cursor: $cursor) {
    edges {
      node {
        id
        token
      }
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
